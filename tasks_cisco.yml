# This tasks file contains the required tasks to backup an cisco switch

---
#- name: Get a full backup of the current running-config
#  ios_config:
#    commands:
#      - show run
#    register: config
  debug:
    var: config

